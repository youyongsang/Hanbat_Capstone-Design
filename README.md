# 유용상 딥러닝 기반 무선 네트워크 최적화 기법 개발

본 프로젝트는 딥러닝을 활용하여 무선 네트워크 환경에서 발생하는 트래픽 패턴을 분석하고,  
이를 기반으로 네트워크 자원을 자동으로 할당하는 최적화 기법을 연구·구현하는 것을 목표로 합니다.

---

## 📌 트래픽 기반 자동 자원 할당 기법

무선 네트워크 환경에서는 시간대, 사용자 수, 서비스 유형에 따라
트래픽 소비 패턴이 지속적으로 변화한다.
본 프로젝트에서는 이러한 트래픽 변화를 딥러닝 모델을 통해 분석하고,
최근 트래픽 사용 패턴을 기반으로 네트워크 자원을
자동으로 분배하는 기법을 제안한다.

본 기법은 단순한 미래 트래픽 값 예측이 아닌,
현재 및 최근 트래픽 패턴을 종합적으로 반영한
네트워크 자원 최적화에 초점을 둔다.

주요 특징은 다음과 같다.

- 최근 트래픽 소비량 및 분포를 활용한 트래픽 패턴 분석

- 딥러닝 기반 자원 배분 결정 및 동적 자원 할당

- 소규모 무선 환경에서도 적용 가능한 경량화 딥러닝 모델 구조 고려

---

## ⚙️ 하드웨어 구성 파악

\* 아래 표는 본 프로젝트 개발 및 실험에 사용된 **로컬 개발 환경 기준 예시**입니다.

| 구분 | 항목 | 내용 |
|------|------|------|
| Hardware | CPU | AMD Ryzen 5 7500F (6-Core, 3.70 GHz) |
| Hardware | RAM | 32GB |
| Software | OS | Windows 11 Pro (24H2, Build 26100.3476) |
| Software | Frontend | PyQt 5.15.10 (GUI Client) |
| Software | Backend | Flask 3.0.3 (Python 3.11) |
| Software | Database | MongoDB 7.0 |
| Software | OTP Library | PyOTP 2.9.0 |
| Software | Web Framework | Flask + Socket 통신 |
| Runtime Environment | IDE | Visual Studio Code 1.93 |
| Runtime Environment | Network | Localhost |
| Runtime Environment | Browser | Chrome 129.0 |

> ℹ️ 본 환경은 개발 및 테스트 목적의 구성으로,  
> 실제 배포 환경에서는 서버 사양 및 네트워크 구조가 변경될 수 있습니다.

---

## 🧠 시스템 구성 개요

본 프로젝트의 시스템은 무선 네트워크 트래픽 패턴을 입력으로 받아,
딥러닝 기반으로 네트워크 자원을 자동 최적화하는 구조로 설계된다.
전체 시스템은 크게 데이터 수집 단계, 딥러닝 기반 분석 단계, 자원 할당 단계로 구성된다.

---

1️⃣ 트래픽 데이터 수집 및 전처리 모듈

무선 네트워크 환경에서 발생하는 트래픽 정보를 주기적으로 수집하여
딥러닝 모델 학습 및 추론에 적합한 형태로 전처리한다.

- 시간 단위별 트래픽 소비량(패킷 수, 전송량 등) 수집

- 노드(단말) 또는 서비스 단위 트래픽 집계

- 이상치 제거 및 정규화 처리

- 최근 트래픽 흐름을 반영하기 위한 시계열 데이터 구성

이를 통해 네트워크의 현재 상태를 정량적으로 표현한 입력 데이터를 생성한다.

---

2️⃣ 딥러닝 기반 트래픽 패턴 분석 모듈

전처리된 트래픽 데이터는 딥러닝 모델에 입력되어
네트워크 트래픽의 시간적 변화 및 사용 패턴을 학습한다.

- 과거 트래픽 소비 패턴을 기반으로 한 네트워크 상태 분석

- 트래픽 집중, 분산, 급증과 같은 패턴 인식

- 소규모 무선 환경에서도 동작 가능하도록 경량 딥러닝 모델 구조 적용

본 단계에서 딥러닝 모델은 단순한 수치 계산이 아닌,
네트워크 상태 전반을 반영한 특징(feature)를 추출하는 역할을 수행한다.

---

3️⃣ 자동 자원 할당 및 최적화 모듈

딥러닝 모델의 출력 결과를 기반으로
네트워크 자원을 동적으로 분배하는 자원 할당 로직을 수행한다.

- 트래픽 소비량 및 패턴을 고려한 자원 분배 비율 결정

- 특정 노드 또는 구간에 대한 자원 과다 집중 방지

- 실시간 네트워크 상태 변화에 따른 자원 재조정

이를 통해 현재 네트워크 트래픽 상황에 적합한 자원 배분이 가능하도록 한다.

---

4️⃣ 시스템 통합 및 시각화 모듈

전체 시스템은 하나의 파이프라인으로 통합되어 동작하며,
자원 할당 결과와 네트워크 상태를 사용자에게 직관적으로 제공한다.

- 트래픽 변화에 따른 자원 할당 결과 시각화

- 시간대별 네트워크 부하 변화 확인

- 실험 및 분석을 위한 로그 기록 기능

해당 모듈을 통해 시스템의 동작 결과를 검증하고 분석할 수 있다.

---

🔁 전체 동작 흐름 요약

    트래픽 데이터 수집
  
          ↓
          
    데이터 전처리 및 시계열 구성
  
          ↓
          
    딥러닝 기반 트래픽 패턴 분석
  
          ↓
          
    자동 자원 할당 및 최적화
  
          ↓
          
  결과 시각화 및 분석

📌 시스템 구성의 특징

- 트래픽 예측 및 패턴 분석을 활용한 지능형 자원 할당

- 규칙 기반이 아닌 딥러닝 기반 네트워크 최적화 구조

- 경량 딥러닝 모델을 통한 실시간 적용 가능성

- 무선 네트워크 환경에 특화된 확장 가능한 설계

## 📁 프로젝트 구조

```
traffic-resource-allocation/
├─ main.py
├─ config.py
├─ README.md
├─ data/
│   ├─ raw/
│   └─ processed/
├─ preprocessing/
├─ models/
├─ train/
├─ utils/
└─ artifacts/
```

---

## ▶️ 실행 방법 (One-Command Pipeline)

```bash
python main.py --check_npz --label_mode next_step --epochs 20
```

---

## 📊 학습 결과

- 최적 모델 저장 위치:
```
artifacts/ys_alloc_net.pt
```

---

## 🚀 향후 계획

- 예측 기반 자원 할당 모델과의 성능 비교
- 공정성 제약(Fairness penalty) 포함 모델 확장
- 실시간 무선 네트워크 환경 적용

## 📂 프로젝트 진행 현황

- [x] 프로젝트 주제 선정
- [x] 개발 환경 구성
- [x] 트래픽 데이터 수집
- [x] 딥러닝 모델 설계 및 학습
- [x] 성능 평가 및 분석
- [ ] 최종 보고서 작성

